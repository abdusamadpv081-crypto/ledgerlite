{
  "name": "@ledgerlite/db",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "generate": "drizzle-kit generate",
    "migrate": "node scripts/migrate.mjs",
    "migrate:baseline": "node scripts/migrate.mjs --baseline",
    "provision:pilot-owner": "node scripts/provision-pilot-owner.mjs",
    "provision:pilot-staff": "node scripts/provision-pilot-staff.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2",
    "pg": "^8.0.0"
  },
  "devDependencies": {
    "@types/pg": "^8.0.0",
    "drizzle-kit": "^0.31.0",
    "vitest": "4.1.10"
  }
}
