# Container notes

Use `infra/compose/docker-compose.yml` for local PostgreSQL and Redis after Docker Desktop is available. Production images and deployment definitions are added only after the first vertical slice runs locally.
